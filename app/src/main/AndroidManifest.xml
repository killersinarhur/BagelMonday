<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.syf.kotlinbagelmonday">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MakeReservation"
            android:label="Reservation" />
        <activity
            android:name=".NaviagationActivity"
            android:label="@string/title_activity_naviagation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".authentucation.SignupActivity" />
        <activity android:name=".authentucation.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".authentucation.ResetPasswordActivity" />
        <activity android:name=".ViewReservation" />
    </application>

</manifest>